.printing-layer-control {
  height: 176px;
  width: 8px;
  position: absolute;
  right: 24px;
  top: 50%;
  transform: translateY(-50%);
  display: none;

  >div {
    height: 100%;
    padding: 0;
  }
}

.isshow {
  display: inline-block;
}